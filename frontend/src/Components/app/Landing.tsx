import { HTMLAttributes } from "react";
import styled from "styled-components";

const Landing = styled((props: HTMLAttributes<HTMLDivElement>) => {
  return <div {...props}></div>;
})``;

export default Landing;
