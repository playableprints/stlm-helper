import styled from "styled-components";

export default styled.h2`
    text-align: center;
    font-variant: small-caps;
    font-weight: 100;
    font-size: 2rem;
    grid-column: 1 / -1;
`;
